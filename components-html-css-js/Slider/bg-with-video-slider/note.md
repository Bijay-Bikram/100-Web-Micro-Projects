
1. element:not(:last-child) => :not pseudo property in CSS

2.  transform: scale(1.3); on hover

3.  mix-blend-mode: multiply; => CSS property that must be used to mix the colors with the backdrop

4. The "clip-path" CSS property creates a clipping region that sets what part of an element should be shown. Parts that are inside the region are shown, while those outside are hidden.

5. The object-fit CSS property sets how the content of a replaced element (parent element) , such as an <img> or <video>, should be resized to fit its container.